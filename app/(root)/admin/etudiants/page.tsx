import EtudiantTable from "@/components/table/EtudiantTable";

const EtudiantPage = () => {
  return <EtudiantTable />; // L'interface de la page etudiant avec la table des etudiants cest a dire la liste des etudiants
};

export default EtudiantPage;
